on:
  workflow_dispatch:

jobs:
  self-destruction:
    runs-on: ubuntu-latest
    steps:
      - run: |
          gh run delete --repo ${{ github.repository }} ${{ github.run_id }}
        env:
          GH_TOKEN: ${{ secrets.GH_ACTIONS_TOKEN }}
