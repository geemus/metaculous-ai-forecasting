Review these predictions for the same question from other superforecasters.
<forecasts>
<%- @forecasts.each do |f| -%>
<%- next if f == @forecast -%>
<forecast>
<%= f.stripped_content('reflect') %>
</forecast>
<%- end -%>
</forecasts>

- Compare your initial forecast to each of these forecasts, identifying the largest differences and root causes.
<%- unless %w[sonar-reasoning sonar-reasoning-pro sonar-deep-research].include?(llm.model) -%>
- Before responding, show step-by-step reasoning in clear, logical order starting with `<think>` on the line before and ending with `</think>` on the line after.
<%- end -%>
- After responding, provide actionable recommendations to improve the prompt's effectiveness with reasoning explanations starting with `<reflect>` on the line before and ending with `</reflect>` on the line after.
- Provide a revised forecast, include your confidence level and note any uncertainties impacting your revision.

- Before your forecast, write:
  - The time remaining before the outcome of the question will become known.
  - The status quo outcome if nothing changed.
  - The expectations of experts and markets.
